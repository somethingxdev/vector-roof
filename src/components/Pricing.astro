---
import { getImage, Image } from 'astro:assets';
import myBackground from '../assets/pricing-bg.png';
import PricingForm from '../components/forms/PricingForm';
const saleBG = await getImage({ src: myBackground, format: 'webp', quality: 100 });
---

<div class="bg-cover sale-form relative bg-no-repeat bg-center min-h-[695px] sm:min-h-0" style={`background-image: url(${saleBG.src});`}>
  <div class="container relative z-20">
    <div class="py-14 lg:py-20">
      <h2 class="section-title text-[#0A0B0F] mb-2">Хотите получить расчет стоимости?</h2>
      <p class="text-[#848896] text-lg mb-8 sm:mb-10">Оставьте чертеж или фото объекта и мы с вами свяжемся</p>
      <PricingForm client:load />
    </div>
  </div>
  <Image src={import('../assets/pricing-mask.png')} class="sm:hidden absolute bottom-0 left-0 w-full h-full z-0" alt="mask" />
</div>
